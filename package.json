{
	"name": "push-notification",
	"version": "1.0.0",
	"description": "Making an app with push notifications.",
	"main": "dist/app/index.js",
	"author": "Sebastiaan Benjamins",
	"license": "MIT",
	"private": true,
	"scripts": {
		"clean": "rm -r dist/*",
		"mkdir": "mkdir dist/client",
		"copy": "cp client/* dist/client",
		"prebuild": "npm run clean && npm run mkdir && npm run copy",
		"build": "tsc",
		"prestart": "npm run build",
		"start": "node ."
	},
	"dependencies": {
		"airtable": "^0.11.2",
		"body-parser": "^1.19.2",
		"dotenv": "^16.0.0",
		"express": "^4.17.3",
		"path": "^0.12.7",
		"typescript": "^4.6.2",
		"web-push": "^3.4.5"
	},
	"devDependencies": {
		"@types/airtable": "^0.10.1",
		"@types/express": "^4.17.13",
		"@types/web-push": "^3.3.2"
	}
}
